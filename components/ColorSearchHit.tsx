// TypeScript interface for the Hit object's structure
export interface HitProps {
        code: string;
        description: string;
        fullName: string;
        hex: string;
        ncsCode: string;
        shortName: string;

}